{% extends 'base.html.twig' %}

{% block body %}

        <h1>{{ acteurs }}</h1>
        <div>
                <a href="{{ path('actor_index') }}">Acteurs</a>
        </div>
        <div>
                <a href="{{ path('actor_show', {'id':"4"}) }}">Alycia Debnam-Carey</a>
        </div>
        <div class="block"></div>
        <h1>{{ categories }}</h1>
        <div>
                <a href="{{ path('form_add') }}">Ajoutez une nouvelle catégorie !</a>
        </div>
        <h1>{{ favorites }}</h1>
        <div>
                <a href="{{ path('wild_show_program', {'programName':"walking-dead"}) }}">Walking dead</a>
        </div>

{% endblock %}
