{% extends 'base.html.twig' %}

{% block body %}
        <h1>{{ acteurs }}</h1>
        <div class="block"></div>
        <h1>{{ categories }}</h1>
        <h1>{{ favorites }}</h1>
        <a href="{{ path('wild_show', {'slug':"ma-superbe-serie"}) }}">Afficher la série</a>

{% endblock %}
